这个搜索的网站是我自己在感觉到单一搜索引擎不能快速满足自我需求是自己编写的一个工具。

在编写这个网站时遇到的主要问题问题就是https协议的Yahoo不能再iframe中通过src=”“属性引用，

这里是先将索引的结果保存到一个HTML文档中，然后在引用src属性，但是这种方法会影响搜索的响应速度，

还有一个没有完成的工作，就是在show页面没有搜索的框，而直接在show中Yahoo是不能搜素的